load "plot-defaults-pdf.gpi"

set xlabel "Log file"
set ylabel "Percentage increase of compiled once over compiled and discarded"
set output "../build/plot-cached-discarded-factor.pdf"
# Add a % sign on the y axis
set format y "% g%%"

plot "../build/stats-cached-discarded-factor.txt" using 1 with lines \
    title "Percentage increase of compiled once over compiled and discarded"
