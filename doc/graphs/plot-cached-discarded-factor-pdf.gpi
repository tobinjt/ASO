load "plot-defaults-pdf.gpi"

set xlabel "Log file"
set ylabel "Increase in parsing time when not caching compiled regexes"
set output "../build/plot-cached-discarded-factor.pdf"
# Add a % sign on the y axis
set format y "% g%%"

plot "../build/stats-cached-regexes-vs-discarded-regexes" using 1 with lines \
    title "Increase in parsing time when not caching compiled regexes"
