load "plot-defaults-ps.gpi"

set xlabel "Log file"
set ylabel "Percentage parsing time increase of \nmaximum ruleset over minimum ruleset"
set output "../build/graph-full-ruleset-vs-minimum-ruleset.ps"

# Add a % sign on the y axis
set format y "% g%%"
set yrange [-4:*]

plot "../build/stats-optimal-full-ruleset-vs-minimum-ruleset" using 2 with lines title "Optimal ordering", \
    "../build/stats-shuffle-full-ruleset-vs-minimum-ruleset" using 2 with lines title "Shuffled ordering", \
    "../build/stats-reverse-full-ruleset-vs-minimum-ruleset" using 2 with lines title "Reversed ordering"
