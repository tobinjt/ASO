load "plot-defaults.gpi"

set xlabel "Rule"
set ylabel "Number of hits"
set output "build/plot-hits.ps"

# Disable the default tics because they're illegible.
set noxtics
set xtics out 0,25,125

plot "stats-hits.txt" using 1:2 title "Number of hits"
